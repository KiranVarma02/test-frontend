import { CreditCardDetails } from './credit-card-details.model';

describe('CreditCardDetails', () => {
  it('should create an instance', () => {
    expect(new CreditCardDetails()).toBeTruthy();
  });
});
